def sub(arr):

    n= len(arr)
    current = arr[0] #2
    max_sum = arr[0] #2

    for i in range(1,n):

        current = max(arr[i] , current + arr[i])
        max_sum = max(max_sum,current)

    return max_sum

print(sub([2,3,-8,7,-1,2,3]))
