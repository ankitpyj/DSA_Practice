def get_min_max(arr):
    min_num = arr[0]
    max_num = arr[0]

    for n in arr[1:]:
        if n < min_num:
            min_num = n
        elif n > max_num:
            max_num = n

    return (min_num, max_num)


print(get_min_max([1, 2, 3, 4, 5]))   # Output: (1, 5)

