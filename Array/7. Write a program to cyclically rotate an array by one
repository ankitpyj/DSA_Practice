# def rotate( arr):
#     n = len(arr)
    
#     return arr[n-1:] + arr[:n-1]
    
# print(rotate([1,2,3,4,5])) 


def rotate(arr,d):
    n = len(arr)

    return arr[d:] + arr[:d]

print(rotate([1,2,3,4,5],4)) 
